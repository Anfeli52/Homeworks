export class Person {
    id: string;
    name: string;
    age: number;
    city: string;

    constructor(id: string, name: string, age: number, city: string) {
        this.id = id;
        this.name = name;
        this.age = age;
        this.city = city;
    }
}